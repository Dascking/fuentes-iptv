name: Keep HF Alive

on:
  schedule:
    - cron: "*/25 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: curl -s -o /dev/null -w "HTTP %{http_code}" https://dasck-easyproxy.hf.space/
